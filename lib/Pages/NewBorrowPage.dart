import 'package:flutter/material.dart';

class NewBorrowerPage extends StatelessWidget {
  const NewBorrowerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}